{
  "extends": "./tsconfig.base.json",
  // Remove the exclusion for test files, let the specific tsconfig handle inclusion/exclusion
  "exclude": ["./tsconfig.*", "../../test"],
  "include": [".."]
}
